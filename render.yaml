# Render deployment for the FastAPI prediction service.
# Create a new "Blueprint" on Render pointing at this repo, or configure a Web
# Service manually with the settings below.
services:
  - type: web
    name: tech-salary-api
    runtime: python
    rootDir: summative/API
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn prediction:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
